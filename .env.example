ANTHROPIC_API_KEY=

# Optional — enables a per-IP daily scan cap (8/day). Without these,
# the app works fine but has no rate limiting. Free tier at upstash.com:
# create a Redis database, copy the REST URL/token from its dashboard.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
